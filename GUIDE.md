# FreelanceHub 사용 설명서

## 목차

1. [시작하기](#1-시작하기)
2. [대시보드](#2-대시보드)
3. [클라이언트 관리](#3-클라이언트-관리)
4. [프로젝트 관리](#4-프로젝트-관리)
5. [견적서](#5-견적서)
6. [정산 / 세금](#6-정산--세금)

---

## 1. 시작하기

### 회원가입

1. `/auth/register` 페이지에서 이름, 이메일, 비밀번호를 입력합니다.
2. 가입 후 자동으로 로그인되어 대시보드로 이동합니다.

### 로그인

**이메일 로그인**
- 가입 시 사용한 이메일과 비밀번호를 입력합니다.

**카카오 로그인**
- `카카오로 로그인` 버튼을 클릭하면 카카오 인증 화면으로 이동합니다.
- 카카오 계정으로 인증 완료 후 자동으로 대시보드로 이동합니다.
- 최초 로그인 시 계정이 자동 생성됩니다.

### 로그아웃

- 좌측 사이드바 하단의 → 아이콘을 클릭합니다.

---

## 2. 대시보드

로그인 후 처음 진입하는 페이지로, 현재 상황을 한눈에 파악할 수 있습니다.

### 수입 요약 카드

| 카드 | 내용 |
|------|------|
| 이번 달 수입 | 당월 실수령 수입 합계 및 전월 대비 증감률 |
| 연간 누적 수입 | 올해 1월부터 현재까지 누적 수입 |
| 미수금 합계 | 아직 입금되지 않은 금액 합계 |
| 전월 수입 | 지난달 실수령 수입 합계 |

### 마감 예정 프로젝트

- 이번 달 내 마감일이 있는 진행 중 프로젝트를 표시합니다.
- **D-3 이하**: 빨간색, **D-7 이하**: 노란색, **그 외**: 초록색 뱃지로 표시됩니다.
- 프로젝트 이름을 클릭하면 상세 페이지로 이동합니다.

### 미수금 현황

- 선금 또는 잔금이 아직 수령되지 않은 프로젝트 목록입니다.
- 미수금 금액이 빨간색으로 표시됩니다.

---

## 3. 클라이언트 관리

거래처(클라이언트) 정보를 등록하고 관리합니다.

### 클라이언트 등록

1. 클라이언트 목록 페이지 우상단 `새 클라이언트` 버튼 클릭
2. 아래 정보를 입력합니다.

| 항목 | 필수 | 설명 |
|------|------|------|
| 업체명 | ✓ | 거래처 이름 |
| 담당자 | | 담당자 이름 |
| 이메일 | | 연락용 이메일 |
| 전화번호 | | 연락처 |
| 플랫폼 | | 계약 경로 (직접계약/크몽/숨고 등) |
| 사업자번호 | | 세금계산서 발행 시 필요 |
| 평점 | | 별점 1~5 (내부 메모용) |
| 메모 | | 자유 형식의 메모 |

### 클라이언트 목록

- **검색**: 이름, 이메일로 실시간 검색
- **블랙리스트 필터**: 전체 / 블랙리스트 제외 / 블랙리스트만 선택 가능
- 이름 클릭 시 상세 페이지로 이동
- 휴지통 아이콘으로 삭제

### 클라이언트 상세

- **기본 정보**: 등록된 연락처, 플랫폼, 사업자번호, 평점 확인
- **통계**: 총 프로젝트 수, 완료 프로젝트 수, 해당 클라이언트로부터의 총 수입
- **프로젝트 히스토리**: 이 클라이언트와 진행한 프로젝트 목록 및 상태
- `새 프로젝트` 버튼: 해당 클라이언트가 미리 선택된 상태로 프로젝트 등록 페이지로 이동
- `편집` 버튼: 정보 수정 및 블랙리스트 설정

---

## 4. 프로젝트 관리

프로젝트의 전체 생애주기를 관리합니다.

### 프로젝트 등록

1. 프로젝트 목록 페이지 우상단 `새 프로젝트` 버튼 클릭
2. 아래 정보를 입력합니다.

| 항목 | 필수 | 설명 |
|------|------|------|
| 프로젝트명 | ✓ | 프로젝트 제목 |
| 클라이언트 | | 연결할 클라이언트 선택 |
| 상태 | | 초기 상태 선택 |
| 계약금액 | | 총 계약 금액 |
| 플랫폼 | | 계약 경로 |
| 선금 | | 선금 금액 |
| 잔금 | | 잔금 금액 |
| 시작일 / 마감일 | | 일정 관리용 날짜 |
| 메모 | | 자유 형식의 메모 |

### 프로젝트 상태 흐름

프로젝트는 다음 순서로 상태가 변경됩니다.

```
문의 → 협의중 → 진행중 → 납품 → 완료
                              ↘ 취소
```

- 프로젝트 상세 페이지 우상단의 드롭다운으로 즉시 변경 가능합니다.
- 목록 탭에서 상태별로 필터링할 수 있습니다.

### 금액 및 정보 편집

프로젝트 상세 페이지 우상단의 `편집` 버튼을 클릭하면 모달이 열립니다.

수정 가능한 항목:

| 항목 | 설명 |
|------|------|
| 계약금액 | 총 계약 금액 |
| 선금 | 선금 금액 |
| 잔금 | 잔금 금액 |
| 플랫폼 | 계약 경로 |
| 시작일 / 마감일 | 일정 변경 |
| 메모 | 자유 형식의 메모 |

`저장` 클릭 시 페이지 새로고침 없이 즉시 반영됩니다.

### 미수금 수령 처리

선금·잔금 카드에 **미수령** 상태인 경우 `수령 완료` 버튼이 표시됩니다.

- 버튼 클릭 시 오늘 날짜로 수령 처리됩니다.
- 처리 후 버튼이 사라지고 상태가 **수령완료**로 변경됩니다.
- 대시보드의 미수금 합계에도 즉시 반영됩니다.

### 체크리스트

프로젝트 내 할 일 목록을 관리합니다.

1. 입력창에 항목명 입력 후 `Enter` 또는 `+` 버튼 클릭
2. 체크박스 클릭으로 완료 처리 (취소선 표시)
3. `✕` 버튼으로 항목 삭제

### 타임트래킹

프로젝트에 소요된 작업 시간을 기록합니다.

1. `시작` 버튼 클릭 → 타이머 시작 (HH:MM:SS 표시)
2. `정지` 버튼 클릭 → 작업 종료, 소요 시간이 로그에 기록
3. 하단에 누적 총 작업시간이 표시됩니다.

---

## 5. 견적서

### 견적서 작성

1. 견적서 목록 페이지 우상단 `새 견적서` 버튼 클릭
2. 단계별로 작성합니다.

**① 프로젝트 선택**
- 견적서를 연결할 프로젝트를 선택합니다.

**② 견적 항목 입력**
- `항목 추가` 버튼으로 행을 추가합니다.
- 각 행에 항목명, 수량, 단가를 입력하면 금액이 자동 계산됩니다.

**③ 금액 요약**

| 항목 | 설명 |
|------|------|
| 공급가액 | 모든 항목 금액의 합계 |
| 부가세 | 10% VAT (체크박스로 포함 여부 선택) |
| 할인 | 차감할 할인 금액 직접 입력 |
| 합계 | 최종 견적 금액 |

**④ 메모**
- 유효기간, 특이사항 등을 자유롭게 입력합니다.

### 공유 링크 발송

클라이언트가 로그인 없이 견적서를 확인하고 수락/거절할 수 있는 링크를 생성합니다.

1. 견적서 상세 페이지에서 `공유링크` 버튼 클릭
2. 만료일을 선택하거나 비워두면(무기한) `링크 생성`
3. 생성된 링크를 `복사` 버튼으로 복사해 클라이언트에게 전달
4. 클라이언트가 링크에서 수락/거절하면 견적서 상태가 자동 업데이트됩니다.

> 링크 형식: `http://localhost:3000/q/{토큰}`

### PDF 다운로드

- 견적서 상세 페이지에서 `PDF` 버튼 클릭
- 브라우저에서 PDF가 열리며 저장할 수 있습니다.

### 견적서 상태

| 상태 | 설명 |
|------|------|
| 초안 | 작성 중, 아직 발송 전 |
| 발송 | 공유 링크 생성 후 클라이언트에게 전달됨 |
| 수락 | 클라이언트가 공유 링크에서 수락함 |
| 거절 | 클라이언트가 공유 링크에서 거절함 |
| 만료 | 설정한 만료일이 지남 |

---

## 6. 정산 / 세금

수입 내역을 기록하고 세금 관련 정보를 확인합니다.

### 수입 추가

`수입 추가` 버튼 클릭 후 아래 항목을 입력합니다.

| 항목 | 필수 | 설명 |
|------|------|------|
| 프로젝트 | ✓ | 수입이 발생한 프로젝트 선택 |
| 유형 | | 전액 / 선금 / 잔금 / 추가 |
| 금액 | ✓ | 수령한 금액 (세전) |
| 지급일 | ✓ | 실제 입금된 날짜 |
| 원천징수 | | 체크 시 3.3% 공제 후 실수령액 자동 계산 |
| 메모 | | 참고 사항 |

**원천징수 적용 시**
- 금액의 3.3%가 자동 차감되어 실수령액이 계산됩니다.
- 예) 100만 원 → 실수령 96만 7천 원

### 필터

- **연도 필터**: 최근 5년 중 선택
- **월 필터**: 특정 월만 조회 (비우면 연간 전체)
- 필터 변경 시 목록과 요약 카드가 함께 업데이트됩니다.

### 요약 카드

| 카드 | 설명 |
|------|------|
| 이번달 | 이번 달 실수령 수입 합계 |
| 올해 누계 | 올해 전체 실수령 수입 합계 |
| 원천징수 납부액 | 해당 연도 원천징수로 공제된 총액 |
| 예상 소득세 | 연 수입 기준 예상 종합소득세 (참고용) |

### 세금 리포트

- 연도별 원천징수 납부 총액과 예상 소득세를 확인합니다.
- 예상 소득세는 단순 참고용이며, 실제 신고 시에는 공인 세무사와 상담을 권장합니다.
